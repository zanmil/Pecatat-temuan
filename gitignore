# Database - jangan di-commit ke git.
# File ini dibuat OTOMATIS oleh aplikasi saat pertama kali dijalankan,
# dan akan terus bertambah isinya seiring pemakaian.
# Kalau ikut di-commit, versi di GitHub akan selalu ketinggalan/beda
# dari database asli yang dipakai aplikasi saat berjalan.
*.db
*.db-journal

# File Excel hasil export (opsional, bisa dihapus baris ini kalau
# memang mau menyimpan hasil export tertentu di git)
data_temuan.xlsx

# Python
__pycache__/
*.pyc
.venv/
venv/

# Streamlit
.streamlit/secrets.toml
